spring:
  profiles:
    active:
      - dev

---
spring:
  profiles: dev
  application:
    name: microservicecloud-config-zmm-dev

---
spring:
  profiles: test
  application:
    name: microservicecloud-config-zmm-test